name: Build Nginx Binary

on:
  workflow_dispatch:

jobs:
  find-releases:
    runs-on: ubuntu-latest
    outputs:
      versions_json: ${{ steps.set-matrix.outputs.versions_json }}
      should_build: ${{ steps.set-matrix.outputs.should_build }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - name: Find Nginx releases to build
        id: set-matrix
        run: |
          # 安装 jq
          if ! command -v jq &> /dev/null; then
            sudo apt-get update && sudo apt-get install -y jq
          fi

          # 获取上游仓库的所有 releases
          echo "Fetching releases from nginx/nginx..."
          releases=$(curl -s https://api.github.com/repos/nginx/nginx/releases)

          # 初始化变量用于存储最新版本
          latest_128=""
          latest_129=""
          
          # 遍历 releases 并查找匹配的版本
          for row in $(echo "${releases}" | jq -r '.[] | @base64'); do
            _jq() { echo ${row} | base64 --decode | jq -r ${1}; }
            tag_name=$(_jq '.tag_name')
            # 格式如 release-1.28.3
            if [[ $tag_name =~ ^release-(1\.28\.[0-9]+|1\.29\.[0-9]+)$ ]]; then
              full_version="${tag_name#release-}"
              if [[ $full_version =~ ^1\.28\.[0-9]+$ ]] && ( [[ -z "$latest_128" ]] || [[ "$full_version" > "$latest_128" ]] ); then
                latest_128="$full_version"
              elif [[ $full_version =~ ^1\.29\.[0-9]+$ ]] && ( [[ -z "$latest_129" ]] || [[ "$full_version" > "$latest_129" ]] ); then
                latest_129="$full_version"
              fi
            fi
          done

          # 构建版本数组（手动/自动均构建最新版本）
          versions=()
          [[ -n "$latest_128" ]] && versions+=("$latest_128")
          [[ -n "$latest_129" ]] && versions+=("$latest_129")

          # 生成紧凑 JSON 数组
          if [ ${#versions[@]} -gt 0 ]; then
            temp_json=$(printf '%s\n' "${versions[@]}" | jq -R . | jq -s -c .)
            should_build="true"
          else
            temp_json='[]'
            should_build="false"
          fi

          echo "versions_json=$temp_json" >> $GITHUB_OUTPUT
          echo "should_build=$should_build" >> $GITHUB_OUTPUT

  build-binary:
    needs: find-releases
    if: needs.find-releases.outputs.should_build == 'true'
    strategy:
      fail-fast: false
      matrix:
        version: ${{ fromJson(needs.find-releases.outputs.versions_json) }}
        arch: [x64, arm64]   # 添加架构维度
    runs-on: ${{ matrix.arch == 'x64' && 'ubuntu-24.04' || 'ubuntu-24.04-arm' }}
    steps:
      - name: Setup Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc make libpcre3-dev zlib1g-dev libssl-dev libgd-dev libgeoip-dev \
            libxml2-dev libxslt1-dev libperl-dev libbz2-dev libexpat1-dev \
            libncurses5-dev libreadline-dev libtinfo-dev wget curl jq

      - name: Download Source Code for ${{ matrix.version }}
        run: |
          NGINX_VERSION="${{ matrix.version }}"
          DOWNLOAD_URL="https://github.com/nginx/nginx/archive/release-${NGINX_VERSION}.tar.gz"
          wget -O "nginx-${NGINX_VERSION}.tar.gz" "$DOWNLOAD_URL"

      - name: Extract Source Code for ${{ matrix.version }}
        run: |
          NGINX_VERSION="${{ matrix.version }}"
          mkdir nginx-src
          tar -xzf "nginx-${NGINX_VERSION}.tar.gz" -C nginx-src --strip-components=1
          ls -la nginx-src/

      - name: Configure and Build for ${{ matrix.version }} on ${{ matrix.arch }}
        working-directory: ./nginx-src
        run: |
          NGINX_VERSION="${{ matrix.version }}"
          ./auto/configure \
            --prefix=/var/apps/Nginx/var \
            --sbin-path=/var/apps/Nginx/target/Nginx \
            --modules-path=/var/apps/Nginx/shares/etc/modules \
            --conf-path=/var/apps/Nginx/shares/etc/nginx.conf \
            --pid-path=/var/apps/Nginx/var/nginx.pid \
            --lock-path=/var/apps/Nginx/var/nginx.lock \
            --http-client-body-temp-path=/var/apps/Nginx/var/cache/client_temp \
            --http-proxy-temp-path=/var/apps/Nginx/var/cache/proxy_temp \
            --http-fastcgi-temp-path=/var/apps/Nginx/var/cache/fastcgi_temp \
            --http-uwsgi-temp-path=/var/apps/Nginx/var/cache/uwsgi_temp \
            --http-scgi-temp-path=/var/apps/Nginx/var/cache/scgi_temp \
            --with-http_ssl_module \
            --with-http_v2_module \
            --with-http_v3_module \
            --with-http_realip_module \
            --with-http_image_filter_module \
            --with-http_sub_module \
            --with-http_dav_module \
            --with-http_mp4_module \
            --with-http_gunzip_module \
            --with-http_gzip_static_module \
            --with-http_auth_request_module \
            --with-http_random_index_module \
            --with-http_secure_link_module \
            --with-http_slice_module \
            --with-http_stub_status_module \
            --with-threads \
            --with-file-aio \
            --with-pcre-jit \
            --with-stream \
            --with-stream_ssl_module \
            --with-mail \
            --with-mail_ssl_module
          make -j$(nproc)

      - name: Extract Binary for ${{ matrix.version }} (${{ matrix.arch }})
        run: |
          NGINX_VERSION="${{ matrix.version }}"
          ARCH="${{ matrix.arch }}"
          cp nginx-src/objs/nginx "nginx-${NGINX_VERSION}-${ARCH}"
          ls -la "nginx-${NGINX_VERSION}-${ARCH}"

      - name: Upload Binary Artifact for ${{ matrix.version }} (${{ matrix.arch }})
        uses: actions/upload-artifact@v4
        with:
          name: nginx-${{ matrix.version }}-${{ matrix.arch }}
          path: nginx-${{ matrix.version }}-${{ matrix.arch }}
